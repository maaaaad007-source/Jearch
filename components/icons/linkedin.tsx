import * as React from "react";

/**
 * LinkedIn glyph drawn in the Lucide stroke style so it sits consistently
 * beside the other icons. Lucide dropped brand icons in v1 and this is the
 * only brand mark the contact cards need, so it lives here rather than pulling
 * in a second icon package.
 */
export function LinkedInIcon({ className, ...props }: React.SVGProps<SVGSVGElement>) {
  return (
    <svg
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth={2}
      strokeLinecap="round"
      strokeLinejoin="round"
      aria-hidden="true"
      focusable="false"
      className={className}
      {...props}
    >
      <path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-4 0v7h-4v-7a6 6 0 0 1 6-6z" />
      <rect width="4" height="12" x="2" y="9" />
      <circle cx="4" cy="4" r="2" />
    </svg>
  );
}
